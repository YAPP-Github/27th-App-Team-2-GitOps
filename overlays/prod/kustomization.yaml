apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - notification-deployment.yaml
  - ingress.yaml
  - ingressroute-https.yaml
  - certificate.yaml
  - monitoring.yaml
  - monitoring-ingressroute.yaml
  - portal.yaml
  - portal-ingressroute-https.yaml
  - sprint.yaml
  - sprint-ingressroute-https.yaml
  # secret.yaml / sprint-secret.yaml 은 .gitignore 처리 - 민감 정보 별도 관리
  # sprint-secret.example.yaml 은 문서용이므로 kustomize에 포함하지 않음
  # - secret.yaml
  # tls-secret.yaml 은 cert-manager Certificate로 대체됨 (deprecated)
  # - tls-secret.yaml
